# ✨ create-errika

> A CLI tool to instantly scaffold a Turborepo-based monorepo with `pnpm`. Skip the setup — start building.

---

## 🚀 What is this?

`create-errika` is a developer-friendly CLI that creates a ready-to-use monorepo setup powered by [Turborepo](https://turbo.build) and `pnpm`. It's designed to save you an hour of boilerplate every time you start a new project.

---

## 📦 Features

- 📁 Preconfigured `apps/` and `packages/` directory structure
- 🛠 Comes with `turbo.json`, `tsconfig.json`, and basic `package.json`
- ⚡ Instant dependency install via `pnpm`
- 💡 Clean DX-focused starter — minimal, fast, extensible

---

## 📥 Installation

You don't install it — you use it directly via `npx`:

```bash
npx create-errika@latest my-app
