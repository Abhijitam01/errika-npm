create-errika
Scaffold a new Turborepo project with pnpm workspaces and a clean structure — instantly.

✨ Features
Ready-to-go Turborepo setup

pnpm workspace configured

Clean monorepo structure (apps, packages)

TypeScript support

Easy CLI command to scaffold

Super fast setup

📦 Installation (Local Dev)
Clone the CLI and link it locally:

bash
Copy
Edit
git clone https://github.com/your-username/create-errika.git
cd create-errika
pnpm install
pnpm build
pnpm link
Now you can run:

bash
Copy
Edit
create-errika my-new-project
🚀 Usage (Published CLI)
When published to npm, users can run:

bash
Copy
Edit
npx create-errika
or

bash
Copy
Edit
pnpm create errika
and follow the prompts to create a new project!

🛠 Commands

Command	Description
pnpm build	Build the TypeScript files
pnpm link	Link the CLI globally
create-errika <project-name>	Scaffold a new project
📝 License
MIT License.

✨ Powered by TurboRepo + pnpm
